name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.https://gist.github.com/vmkmym }}
          TIMEZONE: Asia/Seoul
